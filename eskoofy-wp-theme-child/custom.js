/**
 * Eskoofy Child — custom front-end JavaScript.
 *
 * Runs in the footer with `defer` after the parent's main.js (`esk-main`).
 * Add your own behaviour here so it survives parent theme updates.
 *
 * Example:
 *   document.addEventListener('DOMContentLoaded', function () {
 *     console.log('Eskoofy Child custom script loaded.');
 *   });
 */